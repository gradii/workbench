# WorkbenchDesign

### install
`yarn install`

### live demo
`yarn design:serve`
`yarn workbench:serve`
`yarn backend:serve`
then open `localhost:4200` to visit

### snapshot
![snapshot](snapshot.png)
