export enum WorkingAreaMode {
  BUILDER = 'builder',
  PAINTER = 'painter',
  PREVIEW = 'preview',
  DATA = 'data',
}

export enum WorkingAreaWorkflowMode {
  BACKEND = 'backend',
  FRONTEND = 'frontend'
}
